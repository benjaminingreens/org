import os
import sys

def log():

    return None

def metadata_format_validation():
    """
    Describe function
    """

    # initialise dictonary for file information
    file_info = {
        "file_path": None,
        "directory": None,
        "category": None,
        "valid": None,
    }

    return None

def metadata_content_validation():

    return None

def main():

    # PC: log validation start

    # PC: check if .org/ is present
    # if not, raise error and prompt user to run org init

    x = metadata_format_validation()

    y = metadata_content_validation()

    # PC: log validation end

    return None
